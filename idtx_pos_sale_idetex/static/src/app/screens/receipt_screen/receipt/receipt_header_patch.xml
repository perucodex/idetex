<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="idtx_pos_sale_idetex.ReceiptHeader" t-inherit="point_of_sale.ReceiptHeader" t-inherit-mode="extension">
        
        <!-- [FORCE SIMPLIFICATION] 1. Ocultar logo permanentemente -->
        <xpath expr="//img[hasclass('pos-receipt-logo')]" position="replace">
            <div class="d-none" />
        </xpath>
        
        <!-- [FORCE SIMPLIFICATION] 2. Quitar los datos de la empresa en la cabecera (Dirección, etc.) -->
        <xpath expr="//div[hasclass('d-flex','flex-column','align-items-center')]" position="replace">
            <div class="d-none" />
        </xpath>

    </t>
</templates>
