<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="idtx_pos_sale_idetex.OrderSummary" t-inherit="point_of_sale.OrderSummary" t-inherit-mode="extension">
        <xpath expr="//Orderline" position="replace">
            <t t-if="line.isGroup">
                <GroupOrderline group="line" 
                                onClick="(ev, group) => this.clickGroup(ev, group)"
                                onLineClick="(ev, line) => this.clickLine(ev, line)" />
            </t>
            <t t-else="">
                <!-- Código original de Orderline restaurado para el caso 'else' -->
                <Orderline line="line"
                    onClick="(ev, duration) => this.clickLine(ev, line)"
                    onLongPress="(ev, duration) => this.onOrderlineLongPress(ev, line)">
                    <t t-set-slot="pack-lot-icon">
                        <i t-if="line.getProduct()?.product_tmpl_id?.isTracked()"
                            t-on-click.stop="() => this.editPackLotLines(line)" role="img"
                            t-attf-class="{{ line.hasValidProductLot() ? 'text-success' : 'text-danger'}} fa fa-list line-lot-icon ms-1"
                            t-attf-title="{{ line.hasValidProductLot() ? 'Valid product lot' : 'Invalid product lot'}}" />
                    </t>
                </Orderline>
            </t>
        </xpath>
        <xpath expr="//OrderDisplay" position="attributes">
            <attribute name="groupLines">true</attribute>
        </xpath>
    </t>
</templates>
