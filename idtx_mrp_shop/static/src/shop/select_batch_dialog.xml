<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="idtx_mrp_shop.SelectBatchDialog">
        <Dialog size="'md'" title="props.title" modalRef="modalRef">
            <div class="mt-4">
                <h5 class="fw-bold">Employee</h5>
                <select class="form-select fs-4" t-model="state.selectedEmployee">
                    <option value="" selected="selected">
                        <t t-esc="'Select an employee...'"/>
                    </option>
                    <option t-foreach="employees" t-as="emp" t-key="emp.id" t-att-value="emp.id">
                        <t t-esc="emp.name"/>
                    </option>
                </select>
            </div>
            <div class="mt-4">
                <h5 class="fw-bold">Equipment</h5>
                <select class="form-select fs-4" t-model="state.selectedEquipment">
                    <option value="" selected="selected">
                        <t t-esc="'Select a equipment...'"/>
                    </option>
                    <option t-foreach="equipments" t-as="eqp" t-key="eqp.id" t-att-value="eqp.id">
                        <t t-esc="eqp.name"/>
                    </option>
                </select>
            </div>
            <div class="mt-4">
                <h5 class="fw-bold">Batch</h5>
                <select class="form-select fs-4" t-model="state.selectedBatchId">
                    <option value="" selected="selected">
                        <t t-esc="'Select a batch...'"/>
                    </option>
                    <option t-foreach="batchs" t-as="batch" t-key="batch.id" t-att-value="batch.id">
                        <t t-esc="batch.name"/>
                    </option>
                </select>
            </div>
            <div class="o_select_batch_dialog w-100">
                <t t-set-slot="footer">
                    <button class="btn btn-primary"
                            t-att-disabled="!state.selectedBatchId or !state.selectedEquipment or !state.selectedEmployee"
                            t-on-click="confirm">
                        Confirm
                    </button>
                </t>
            </div>
        </Dialog>
    </t>
</templates>