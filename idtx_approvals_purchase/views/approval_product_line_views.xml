<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="approval_product_line_view_tree_inherit" model="ir.ui.view">
        <field name="name">approval.product.line.view.list.inherit</field>
        <field name="model">approval.product.line</field>
        <field name="inherit_id" ref="approvals.approval_product_line_view_tree"/>
        <field name="arch" type="xml">
            <field name="product_id" position="before">
                <field name="sequence" widget="handle"/>
                <field name="item" readonly="1" optional="hide"/>
            </field>
        </field>
    </record>

    <record id="approval_request_view_form_inherit" model="ir.ui.view">
        <field name="name">approval.request.view.form.inherit</field>
        <field name="model">approval.request</field>
        <field name="inherit_id" ref="approvals.approval_request_view_form"/>
        <field name="arch" type="xml">
            <button name="action_draft" position="attributes">
                <attribute name="invisible">request_status != 'cancel'</attribute>
            </button>
        </field>
    </record>

</odoo>
